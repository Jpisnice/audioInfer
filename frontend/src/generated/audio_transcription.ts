// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               v6.33.4
// source: audio_transcription.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "./typeRegistry";

export const protobufPackage = "transcription";

export interface AudioChunk {
  $type: "transcription.AudioChunk";
  audioData: Buffer;
  sampleRate: number;
  channels: number;
}

export interface TranscriptionResponse {
  $type: "transcription.TranscriptionResponse";
  text: string;
  isFinal: boolean;
  confidence: number;
  timestamp: number;
}

function createBaseAudioChunk(): AudioChunk {
  return { $type: "transcription.AudioChunk", audioData: Buffer.alloc(0), sampleRate: 0, channels: 0 };
}

export const AudioChunk: MessageFns<AudioChunk, "transcription.AudioChunk"> = {
  $type: "transcription.AudioChunk" as const,

  encode(message: AudioChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audioData.length !== 0) {
      writer.uint32(10).bytes(message.audioData);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(16).int32(message.sampleRate);
    }
    if (message.channels !== 0) {
      writer.uint32(24).int32(message.channels);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AudioChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAudioChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audioData = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sampleRate = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.channels = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AudioChunk {
    return {
      $type: AudioChunk.$type,
      audioData: isSet(object.audioData)
        ? Buffer.from(bytesFromBase64(object.audioData))
        : isSet(object.audio_data)
        ? Buffer.from(bytesFromBase64(object.audio_data))
        : Buffer.alloc(0),
      sampleRate: isSet(object.sampleRate)
        ? globalThis.Number(object.sampleRate)
        : isSet(object.sample_rate)
        ? globalThis.Number(object.sample_rate)
        : 0,
      channels: isSet(object.channels) ? globalThis.Number(object.channels) : 0,
    };
  },

  toJSON(message: AudioChunk): unknown {
    const obj: any = {};
    if (message.audioData.length !== 0) {
      obj.audioData = base64FromBytes(message.audioData);
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    if (message.channels !== 0) {
      obj.channels = Math.round(message.channels);
    }
    return obj;
  },

  create(base?: DeepPartial<AudioChunk>): AudioChunk {
    return AudioChunk.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AudioChunk>): AudioChunk {
    const message = createBaseAudioChunk();
    message.audioData = object.audioData ?? Buffer.alloc(0);
    message.sampleRate = object.sampleRate ?? 0;
    message.channels = object.channels ?? 0;
    return message;
  },
};

messageTypeRegistry.set(AudioChunk.$type, AudioChunk);

function createBaseTranscriptionResponse(): TranscriptionResponse {
  return { $type: "transcription.TranscriptionResponse", text: "", isFinal: false, confidence: 0, timestamp: 0 };
}

export const TranscriptionResponse: MessageFns<TranscriptionResponse, "transcription.TranscriptionResponse"> = {
  $type: "transcription.TranscriptionResponse" as const,

  encode(message: TranscriptionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    if (message.confidence !== 0) {
      writer.uint32(29).float(message.confidence);
    }
    if (message.timestamp !== 0) {
      writer.uint32(32).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TranscriptionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTranscriptionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.confidence = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TranscriptionResponse {
    return {
      $type: TranscriptionResponse.$type,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      isFinal: isSet(object.isFinal)
        ? globalThis.Boolean(object.isFinal)
        : isSet(object.is_final)
        ? globalThis.Boolean(object.is_final)
        : false,
      confidence: isSet(object.confidence) ? globalThis.Number(object.confidence) : 0,
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: TranscriptionResponse): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    if (message.confidence !== 0) {
      obj.confidence = message.confidence;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create(base?: DeepPartial<TranscriptionResponse>): TranscriptionResponse {
    return TranscriptionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TranscriptionResponse>): TranscriptionResponse {
    const message = createBaseTranscriptionResponse();
    message.text = object.text ?? "";
    message.isFinal = object.isFinal ?? false;
    message.confidence = object.confidence ?? 0;
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

messageTypeRegistry.set(TranscriptionResponse.$type, TranscriptionResponse);

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
